{
  "name": "markdown-blog",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny markdown blog. Posts are .md files, rendered to static HTML by build.js.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "marked": "^18.0.9"
  }
}
